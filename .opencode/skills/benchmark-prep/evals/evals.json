[
  {
    "invocation": ".opencode/skills/benchmark-prep/scripts/prepare_test_dir.sh test2",
    "expect": "exit 0; test2 is its own git repo whose ONLY tracked entry is the .opencode submodule pinned to committed harness HEAD; .opencode/{agents,skills,opencode.jsonc} checked out (no symlinks anywhere); node_modules/godot-mcp-runtime present and invisible to git status in both repos; 'READY:' prints pinned SHA and launch cmd; no icon.svg or other seed files (setup-project creates those during the run)"
  },
  {
    "invocation": ".opencode/skills/benchmark-prep/scripts/prepare_test_dir.sh test2 (second run, after sandbox contains GAME_STATE.md, scenes/, plans/, nested game .git from a dead run)",
    "expect": "exit 0; all run artifacts gone; identical verified-ready state (idempotence + destructiveness)"
  },
  {
    "invocation": ".opencode/skills/benchmark-prep/scripts/prepare_test_dir.sh test2 (with uncommitted harness changes present)",
    "expect": "exit 0; submodule pinned to last COMMITTED HEAD — uncommitted changes excluded (documented gotcha, not a failure)"
  }
]
